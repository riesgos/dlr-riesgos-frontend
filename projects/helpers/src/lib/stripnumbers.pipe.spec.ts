import { StripnumbersPipe } from './stripnumbers.pipe';

describe('StripnumbersPipe', () => {
  it('create an instance', () => {
    const pipe = new StripnumbersPipe();
    expect(pipe).toBeTruthy();
  });
});
